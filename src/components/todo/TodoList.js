import TodoForm from './TodoForm';
import TodoItem from './TodoItem';

const TodoList = () => {
  return (
    <div>
      <TodoForm />
      <TodoItem />
    </div>
  );
};

export default TodoList;
